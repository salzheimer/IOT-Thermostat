# WiFi Configuration
SSID=YourWiFiNetworkName
Password=YourWiFiPassword

# Azure IoT Hub Configuration
IOT_HUB_HOSTNAME=your-iot-hub-name
DEVICE_ID=your-device-id
SAS_TOKEN=your-sas-token-here

# Instructions:
# 1. Copy this file to config.txt
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit config.txt to version control (it's in .gitignore)
#
# To generate a SAS token for Azure IoT Hub, use the Azure CLI or IoT Explorer
# Token format: sr=<hub>.azure-devices.net%2Fdevices%2F<device>&sig=<signature>&se=<expiry>
